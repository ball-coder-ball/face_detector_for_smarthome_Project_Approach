---

### 3. 🚀 วิธีการรัน (Deployment Steps)

เพื่อให้ระบบ Chatbot ทำงาน และ Webhook ส่งกลับมาหาเราได้ คุณต้องรัน 3 อย่างพร้อมกันครับ

#### Terminal 1: รัน Backend (Python)
```bash
# อย่าลืม activate venv ก่อน
python backend_main.py
```
*รอจนขึ้นว่า `Application startup complete.`*

#### Terminal 2: รัน Tunnel (ngrok)
เพื่อให้ LINE ส่งข้อมูลกลับมาที่เครื่องเราได้
```bash
ngrok http 8000